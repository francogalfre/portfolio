---
import { getLangFromURL } from "../../i18n";

const lang = getLangFromURL(Astro.url);
---

<a class="text-black/80 dark:text-white/80" href={lang == "es" ? "/en" : "/"}
  >{lang == "es" ? "español" : "english"}</a
>
